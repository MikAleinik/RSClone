import IController from "../../interfaces/i-controller";
import { AppEvents } from "../app-events";

export default class RegisterController implements IController {
    sendEvent(nameEvent: AppEvents) {

    }
}
